# 運用ルール・安全制御一覧

## 絶対に守ること

### 1. 実装責任の分離
- **AIチャット（実装管理担当）は指示・確認・報告のみ**
- 実装コードは Antigravity が100%担当
- ローカルでの実装作業は一切行わない

### 2. 圧縮出力の義務

\\\
❌ 悪い例：
[Antigravity からの長い作業ログをそのまま上位へ]

✅ 良い例：
【実装結果サマリ】
- 作成/変更ファイル: ServiceA.py, ConfigB.yaml, 他2件
- migration: alembic_rev_20260524_001 (適用済)
- pytest: 合計45件 / 成功45件 / 失敗0件
\\\

### 3. 仕様の安定性
- **一度確定した仕様は勝手に変更しない**
- 変更が必要な場合：
  1. その旨を明示
  2. 理由を記載
  3. 上位に報告
  4. 承認後に実装指示

### 4. 重要制御の維持

#### 4.1 seller/environment guard
\\\
【守るべき内容】
- seller パラメータのバリデーション
- environment（prod/staging/dev）のチェック
- guard logic による不適切な操作防止

【壊さない条件】
- バリデーション順序を変えない
- 例外処理を削除しない
- guard の側道を作らない
\\\

#### 4.2 dry_run / idempotent
\\\
【dry_run の要件】
- --dry-run フラグで実装が可能
- 実際の状態変更がない
- 実行結果は同じ（ログ/メッセージ）

【idempotent の要件】
- 同じ操作を2回実行しても結果は同じ
- 初回と2回目で副作用がない
- ステートレスな設計
\\\

#### 4.3 安全制御の bypass 禁止
\\\
❌ 許されない変更：
- environment guard をスキップする hidden flag 追加
- seller チェックを削除
- 例外処理を無視する try-except
- デバッグモード時の制御削除

✅ 許される変更：
- ログレベルの追加
- 既存ガード条件の強化
- 新しい環境タイプの追加（ガード組み込み）
\\\

### 5. Open Question の即座報告

\\\
【判定フロー】

実装指示を作成
    ↓
[Open Question あり？]
    ├─ YES → 即座に上位へ報告
    │        （実装を進めない）
    └─ NO  → Antigravity へ投げる
\\\

## エスカレーション判定マトリクス

| 状況 | 判定 | アクション |
|------|------|-----------|
| Open Question 残存 | **エスカレーション** | 即座に上位報告 |
| seller/guard/safety の変更が必要 | **エスカレーション** | 即座に上位報告 |
| pytest 失敗が2回以上で未解決 | **エスカレーション** | 根本原因を上位報告 |
| 設計が複数レイヤに波及 | **エスカレーション** | 影響範囲を上位報告 |
| 完了判定を求める | **エスカレーション** | テンプレで報告 |
| 次フェーズ着手判定を求める | **エスカレーション** | テンプレで報告 |
| その他（軽微なギャップ等） | **自己完結** | 報告テンプレで上位判断 |

## タスク管理フロー

\\\
┌─────────────────────────────────────────┐
│ 1. タスク受取（上位から指示）            │
└────────────┬────────────────────────────┘
             ↓
┌─────────────────────────────────────────┐
│ 2. 実装指示作成                         │
│    - ANTIGRAVITY_TEMPLATE.md 使用       │
│    - Open Question チェック             │
└────────────┬────────────────────────────┘
             ↓
┌─────────────────────────────────────────┐
│ 3. Antigravity へ投げる                │
└────────────┬────────────────────────────┘
             ↓
┌─────────────────────────────────────────┐
│ 4. 完了報告を受け取る                   │
│    - 8項目を必ず回収                    │
└────────────┬────────────────────────────┘
             ↓
┌─────────────────────────────────────────┐
│ 5. 一次レビュー                         │
│    - 完了条件をチェック                 │
│    - pytest 成否を確認                  │
│    - commit/push を確認                 │
└────────────┬────────────────────────────┘
             ↓
         [エスカレーション判定]
         ↙       ↓        ↖
       YES       NO        報告
        ↓         ↓         ↓
     上位報告  テンプレ圧縮 上位判断
\\\

## 証跡管理

### 必須回収項目

1. **ファイル一覧** — 新規/変更ファイル
2. **主要クラス一覧** — 実装した重要コンポーネント
3. **migration revision ID** — alembic/flyway 等の ID
4. **pytest 結果** — 成功数/失敗数/カバレッジ
5. **commit message** — 変更内容の1行要約
6. **commit hash** — git commit の hash 値
7. **push 成否** — リモート反映の可否
8. **未解決事項** — 残課題・制約事項

### 保存ルール

- 各フェーズ完了時に証跡を **phase-X-log.md** に記録
- commit hash と pytest 結果のリンク
- エスカレーション判定結果も併記

## FAQ

**Q: 実装中に設計の問題を見つけたら？**
A: Antigravity に指示を出し、上位へ即座にエスカレーション。実装を止める。

**Q: pytest が失敗した場合の判定は？**
A: 1回目は Antigravity に修正させる。2回目以上の失敗なら上位へ報告。

**Q: dry_run を壊さない変更って具体的に何？**
A: --dry-run フラグが効く状態を維持する。つまり、DB変更がない状態で同じ出力。

**Q: Open Question がある場合、指示を出してもいい？**
A: **NO**。絶対に上位に報告してから。指示は保留。

**Q: 完了判定は AIチャット がやる？**
A: NO。報告テンプレで「完了判定可否」を示して、上位に判定させる。

---
*Generated: 2026-05-24 16:30:53*
